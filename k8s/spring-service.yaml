apiVersion: v1
kind: Service
metadata:
  name: spring-service
  namespace: student-app
spec:
  type: NodePort
  selector:
    app: spring-boot
  ports:
    - port: 8080        
      targetPort: 8080 
      nodePort: 30080  